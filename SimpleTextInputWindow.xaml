<Window x:Class="ImageAndMp4WebBuilder.SimpleTextInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="150" Width="400" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Text="Enter text:" />
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBox x:Name="InputBox" Width="250" Margin="0,0,10,0" />
            <Button Content="OK" Width="60" IsDefault="True" Click="Ok_Click" Margin="0,0,5,0" />
            <Button Content="Cancel" Width="60" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>